OVERVIEW

pyapi is a maintenance toolkit that allows your code to stay up to date on whatever python libs/apis you want automatically. it saves your updates as a commit, so all you need to do is review and push.
- pure python, no dependencies
- can configure most* libraries and apis

INSTALLATION
